# コラム & OGP デザインルール

`service.ugcmaker.jp` のお役立ちコラム（連載「EC広告クリエイティブの最新トレンド」）の
**第1〜3回を基準（リファレンス）** として策定したデザインルール。
第7回以降を追加する際は、本ルールに従って一貫性を保つこと。

- 対象: コラム記事ページ（`column-N.html`）と そのOGP画像（`lp-videos/colN-ogp.png`）
- 関連: ブランドの基礎トークンは `README.md` の Design System を参照（本書はコラム固有の上乗せルール）

---

## 1. ブランド / カラートークン

| 用途 | 値 | 備考 |
|---|---|---|
| プライマリー（ブランド） | `#1A1A1A`（黒・`#111`系） | ロゴ・基調色。**ロゴは必ず黒** |
| アクセント / アクション | `#4285F4`（青） | ボタン・リンク・図版の主役色 |
| アクセント濃 | `#1967D2` | ホバー・図版の濃い青 |
| CTAアクセント | `#F54927` / `#F54624`（コーラル） | **CTA・eyebrow・VOL等の差し色のみ**。多用しない |
| 黄ハイライト | `#FFE600` | 本文マーカー（`<strong class="hl">`） |
| 本文テキスト | `#333` | |
| OGP背景 | `#E9F1FC`（淡い青） | |
| OGP装飾円 | `#D0E0F6` | |
| OGPタイトル色 | `#102D57`（ネイビー） | |

> 注: コラムOGPのタイトル/VOLは「ネイビー＋コーラル」の配色だが、**ロゴ（UGC Maker）だけは正式ロゴの黒 `#1A1A1A`** を使う。

---

## 2. タイポグラフィ

| 区分 | フォント | ウェイト |
|---|---|---|
| 和文（本文・見出し） | **Noto Sans JP** | 400 / 700 / **900**（中間は使わない） |
| 英数字・ラベル・ロゴ | **Nunito Sans** | 800 / 900 |

- 見出し・タイトルの「強」は **900（Black）**、「中」は 700。**Bold(700)で済ませない箇所に注意**（OGPタイトルは900）。
- 数字・英字（実績数値、VOL、ロゴ）は Nunito Sans。

---

## 3. OGP画像ルール（`colN-ogp.png` / 1200×630）

第1〜3回のレイアウトを基準とする。座標は viewBox `0 0 1200 630` 基準。

### レイアウト要素

| 要素 | 内容 | フォント / ウェイト | サイズ | 色 | 位置 |
|---|---|---|---|---|---|
| 背景 | 全面 | – | – | `#E9F1FC` | 全面 |
| 装飾円 | 右側に大きな円（右端で見切れる） | – | – | `#D0E0F6` | 中心 ≈ (1040, 315), 半径 ≈ 335 |
| ロゴ | `UGC Maker`（正式ワードマーク） | Nunito Sans **900** / letter-spacing -2相当 | 幅 ≈ 150px | **`#1A1A1A`（黒）** | 右上・右端 x≈1133 / 縦中心 y≈72 |
| eyebrow | `EC広告クリエイティブの最新トレンド`（固定） | Noto Sans JP 700 | 28 | `#F54624` | x86 / y180 |
| タイトル | 記事タイトル（H1と一致・2行） | Noto Sans JP **900** | 56（長い場合50前後） | `#102D57` | x84 / 1行目 y262・2行目 y334（行送り72） |
| VOL | `VOL.0N` | Nunito Sans 800 | 26 | `#F54624` | x86 / y560（左下） |
| イラスト | 記事テーマのフラットイラスト | – | – | 下記パレット | 円の中（translate ≈ 1040,315） |

### OGPタイトルの作り方
- **記事H1と一致させる**（第1回・第3回方式: H1をそのまま2行に折る）。句点「。」は **1行目末尾**に置く（第1回「画像広告はもう限界か。／動画が成果を出す時代へ」に倣う）。
- H1が長く2行に収まらない場合のみ要約する（第2回が例外）。
- **棒線「――」は使わない**（句点「。」区切り。本文中の挿入ダッシュは可）。
- 文字数に応じてフォントサイズを調整（標準56、長い場合は50前後まで縮小）。円のイラスト（左端 ≈ x830〜900）と被らないよう、タイトルは概ね x84〜810 に収める。

### イラスト方針（フラットイラスト）
- スタイル: 角丸・面塗りのフラット。記事テーマを象徴するモチーフ1点を円内に配置。
- パレット: 青 `#4285F4` / 濃青 `#1967D2` / 黄 `#FBBC04` / コーラル `#F54624` / 白。
- 接地影として薄青 `#b9cdee`(opacity 0.5) の楕円を足元に敷く。
- 例: 第1回=A/B動画カード, 第2回=スマホ+リアクション, 第3回=ビル+歯車+人, 第4回=縦型動画+稲妻フック+2秒バッジ, 第5回=A/Bカード+改善ループ, 第6回=AIチップ+きらめき。

### 生成手順（参考: 第4〜6回の実装）
1. SVGで上記レイアウトを組む（フォントは Noto Sans JP / Nunito Sans を OS にインストール）。
2. `rsvg-convert -w 1200 -h 630 in.svg -o colN-ogp.png` でPNG化。
3. ロゴは**公式ワードマークを別途PNG化して合成**すると全記事で完全一致する
   （ヘッダーのインラインSVGロゴ = Nunito Sans 900 / -2 / `#1A1A1A` と同一）。
   - 例: `magick colN-ogp.png logo150.png -geometry +983+61 -composite colN-ogp.png`

---

## 4. コラム記事ページ構造（`column-N.html`）

第3回（`column-3.html`）を雛形とする。共通シャーシ（ヘッダー/CSS/フッター/お問い合わせ/スティッキーCTA/フォーム）はそのまま流用し、記事固有部分だけ差し替える。

### 上から下への構成（`cs-` プレフィックスのクラス体系）
1. パンくず（`cs-breadcrumb`）: HOME › お役立ちコラム › 記事名
2. 日付・読了（`cs-date`）: `公開日 YYYY.MM.DD・更新日 YYYY.MM.DD・読了 約N分`
3. シリーズ表記: `第N回 EC広告クリエイティブの最新トレンド`（青・display）
4. H1 タイトル（`cs-title`）
5. ヒーロー（`cs-hero`）= OGP画像（`colN-ogp.png`）
6. 要点ボックス（`cs-tldr`）: リード文1 + 箇条書き4点
7. 目次（`cs-toc` モバイル / `cs-toc-desktop` サイド）
8. 本文（`cs-body`）: H2(`#tocN`) / H3 / 段落 / 図（`cs-figure`）
9. FAQ（`cs-faq`）: `<details>` で4問
10. CTA（`cs-promo`）: 見出し + 3段落 + 資料DL/相談ボタン
11. 参考・出典 + 注記
12. 著者欄（`cs-writer`）: 黍田 龍平（固定）
13. 関連記事ナビ（`cs-relnav`）: 前の記事 / 次の記事

### 本文の装飾
- **黄マーカー**: 重要な数値・主張を `<strong class="hl">…</strong>` で強調。
  目安として **1記事あたり 1,000字に2〜4箇所程度**（数値・断定が映える箇所に集中）。
- 小見出し的なリード文の頭出しは `<span class="q">…。</span>`（太字・非マーカー）。
- 図版: `<figure class="cs-figure"><img …><figcaption>【図N】… 出典：…</figcaption></figure>`

### メタ情報（記事ごとに必須更新）
- `<title>` / `description` / `keywords` / `canonical`
- OGP（`og:title`=`{H1}｜UGC Maker` / `og:description` / `og:image`=`colN-ogp.png` / `og:url`）/ Twitterカード
- JSON-LD 3種: `BlogPosting`（headline/description/image/datePublished/author/publisher）, `BreadcrumbList`, `FAQPage`

---

## 5. タイトル表記ルール（記事H1・カード・メタ共通）

- **棒線「――」「—」は使わない**。2要素は句点「。」で区切る。
  - 例: `勝てる動画クリエイティブの「型」。冒頭2秒で決まる構成設計`
- 1記事で表記は統一（H1 / `<title>` / OGPメタ / JSON-LD / パンくず / 関連ナビ / トップのカードすべて同一文字列）。
- 本文中の挿入的なダッシュ（補足の `――`）は可。タイトルのみ禁止。

---

## 6. ファイル命名・連番

| 種別 | 命名 | 例 |
|---|---|---|
| 記事 | `column-N.html`（第1回のみ `column.html`） | `column-4.html` |
| OGP | `lp-videos/colN-ogp.png` | `lp-videos/col4-ogp.png` |
| 図版 | `lp-videos/colN-fig{1,2,3}.png` | `lp-videos/col4-fig1.png` |

- 連番ナビ（関連記事）は 前=`column-(N-1)`, 次=`column-(N+1)`。最終回は「次」を空に。
- 追加時は `sitemap.xml` に `column-N.html` を追記、`index.html` のコラムカードを1枚追加。

---

## 7. 公開フロー

1. ローカル確認: `python3 -m http.server 8765` → `http://localhost:8765/`
2. コミット（コラム関連のみ: 記事HTML・`colN-*`画像・`sitemap.xml`）
3. 本番反映: リポルートで `vercel --prod`（GitHub自動デプロイは未設定・手動）

---

_基準: 第1〜3回。最終更新: 2026-06-19（第4〜6回追加・OGPロゴ統一・タイトル棒線廃止を反映）_
